P2P: generic Port still has pointer to Spec::Interface. kill it.
P2P: add dedicated export object to system node (needed for proper hierarchy?)
P2P: protocol encapsulation
P2P: node creation methodology:  1) construct and hook up,   2) configure()
VLOG: iimpl has redundant get_module_name() thing, but also sets name of vlogmodule in implement()
VLOG: impl Port->Port mapping is a nightmare. make it data-driven somehow. same for parameters.
VLOG: give opaque info to vlog instances, vlog ports, vlog modules <<-- or make vlog module subclassed!
VLOG: partial binding of net->port and partial port->net please
VLOG: do the port-binding functions really need to return a PortBinding* ?

P2P: FIX CONST VALUES: it'a state of a field of a specific port/instance. is this right?
VLOG: FIX CONST VALUES: make consts a new kind of net/portbindablethingy
VLOG: FIX CONST VALUES: impl_sys_node is hitler right now